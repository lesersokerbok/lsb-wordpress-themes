<section class="lsb_section is-{{ section.post_type }}">
	<div class="lsb_section__header">
		<h2 class="lsb_section__title">
			{% if section.title.link %}
				<a href="{{ section.title.link }}">{{ section.title.text }}</a>
			{% endif %}
			<span>{{ section.subtitle }}</span></h2>
	</div>
	<div class="lsb_section__body">
		{% for post in section.lsb_posts %}
			{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] with {'h_size': 'h3'} %}
		{% endfor %}
	</div>
</section>
